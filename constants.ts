// Menu options for navigation
export const MENU_OPTIONS = ['Home', 'Courses', 'Practice', 'Progress', 'Settings'];

// Responsive breakpoints
export const MOBILE_BREAKPOINT = 900;

// Session timeout (in minutes)
export const SESSION_TIMEOUT_MINUTES = 60;

// Session timeout (in milliseconds)
export const SESSION_TIMEOUT_MILLISECONDS = SESSION_TIMEOUT_MINUTES * 60 * 1000;

// Cookie names
export const USER_SESSION_COOKIE = 'userSession'; 